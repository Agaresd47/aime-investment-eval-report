# AIME Investment Eval Report

This repository hosts the rendered evaluation report for GitHub Pages.

## GitHub Pages

Use `main` / root as the Pages source. The public entrypoint is:

```text
index.html
```

The report is self-contained except for the local `annotations/` image assets in this repository. It does not require external JavaScript or CDN access.
